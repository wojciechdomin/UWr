aframe(long, long, long*):
        pushq   %rbp
        movq    %rsp, %rbp
        subq    $16, %rsp
        leaq    15(,%rdi,8), %rax
        andq    $-16, %rax
        subq    %rax, %rsp
        movq    %rsp, %rcx
        leaq    -8(%rbp), %rax
        movq    %rax, -8(%rsp,%rdi,8)
        movq    $0, -8(%rbp)
        jmp     .L2
.L3:
        movq    %rdx, (%rcx,%rax,8)
        addq    $1, -8(%rbp)
.L2:
        movq    -8(%rbp), %rax
        cmpq    %rdi, %rax
        jl      .L3
        movq    (%rcx,%rsi,8), %rax
        movq    (%rax), %rax
        leave
        ret

long aframe(long n, long idx, long *q) {
 long i;
 long *p[n];
 p[n-1] = &i;
 for (i = 0; i < n; i++)
 p[i] = q;
 return *p[idx];
}